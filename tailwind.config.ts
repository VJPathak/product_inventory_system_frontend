// Tailwind is NOT used in this project - we use SCSS instead
// This file is kept for compatibility but Tailwind classes are not applied
import type { Config } from "tailwindcss";

export default {
  darkMode: ["class"],
  content: [],
  prefix: "",
  theme: {},
  plugins: [],
} satisfies Config;
